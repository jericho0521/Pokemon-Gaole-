package PokemonApp;

public class GhostPokemon extends Pokemon {
    public GhostPokemon(String name, int hp, int attackPower, int maxHp, int grade) {
        super(name, "Ghost", hp, attackPower, "Shadow Ball", "Ghost", maxHp, grade);
    }
}